"""
2.a Écrire la méthode heigth() pour la classe BinaryTree de sorte qu'elle
retourne la hauteur de l'arbre,
c’est à dire le nombre d’éléments contenus dans la plus longue branche

2.b En s'inspirant dans la méthode height(), implémenter la méthode depth(key: object),
qui calcule la profundeur d'un noeud dans l'arbre
    - la méthode retourne -1 si le node n'est pas trouvé

    NB:
    profondeur : la longueur du chemin reliant un nœud à la racine (pour la racine c’est donc 0)
    hauteur : la profondeur maximale de ses nœuds
              - un arbre à un seul nœud est de hauteur 0
              - l’arbre vide est de hauteur −1
"""